services:
  - type: web
    name: buzz-in-server-1
    env: node
    plan: free
    rootDir: .
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: 18
