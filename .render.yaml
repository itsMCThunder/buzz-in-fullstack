services:
  - type: web
    name: buzz-in
    env: node
    plan: free
    buildCommand: |
      npm ci
      npm run build
    startCommand: |
      npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
